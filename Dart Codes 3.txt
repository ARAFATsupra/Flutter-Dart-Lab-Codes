import 'package:flutter/material.dart';

void main (){
  runApp(MyApp());
}


class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: FirstPage(),

    );
  }
}


class FirstPage extends StatelessWidget {
  const FirstPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
      appBar: AppBar(
        title: Text("Welcome to the App"),
        backgroundColor: Colors.blue,
        foregroundColor: Colors.white,
        centerTitle: true,


      ),
      body: Column(

        children: [

          SizedBox(height: 50,),
          Text(
            "ITM 8th Batch!",
            style: TextStyle(color: Colors.green, fontSize: 22, fontWeight: FontWeight.w800),

          ) ,
          SizedBox(height: 50,),

          Text(
            "ITM 8th Batch!",
            style: TextStyle(color: Colors.red, fontSize: 22, fontWeight: FontWeight.w800),

          ) , SizedBox(height: 50,),
          Text(
            "ITM 8th Batch!",
            style: TextStyle(color: Colors.green, fontSize: 22, fontWeight: FontWeight.w800),

          ) ,SizedBox(height: 50,),
          Text(
            "ITM 8th Batch!",
            style: TextStyle(color: Colors.green, fontSize: 22, fontWeight: FontWeight.w800),

          ) ,SizedBox(height: 50,),
          Text(
            "ITM 8th Batch!",
            style: TextStyle(color: Colors.green, fontSize: 22, fontWeight: FontWeight.w800),

          ) ,SizedBox(height: 50,),

        ],

      ),

    );
  }
}