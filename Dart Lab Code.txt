import 'package:flutter/material.dart';

void main(){
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: FirstPage(),
    );
  }
}

class FirstPage extends StatelessWidget {
  const FirstPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Flutter layout demo'),
        centerTitle: true,
      ),

      body: Column(
        mainAxisAlignment: MainAxisAlignment.start,
        children: [
          Row(
            mainAxisAlignment: MainAxisAlignment.spaceEvenly,
            children: [
              Image.network('https://picsum.photos/1000/300.webp'),
            ],
          ),

          SizedBox(height: 25, width: 20),

          Row(
            mainAxisAlignment: MainAxisAlignment.start,
            children: [
              SizedBox(height: 20, width: 40),
              Text('Oeschinen Lake Campground'),
            ],
          ),

          SizedBox(height: 10, width: 20),

          Row(
            mainAxisAlignment: MainAxisAlignment.start,
            children: [
              SizedBox(height: 20, width: 40),
              Text('Arafat, Dhaka', style: TextStyle(color: Colors.grey),),
              SizedBox(height: 20,width: 790),
              Icon(Icons.star),
              Text('41'),
            ],
          ),


          SizedBox(height: 50, width: 20),

          Row(
            mainAxisAlignment: MainAxisAlignment.spaceEvenly,
            children: [
              Icon(Icons.call),
              SizedBox(height: 20),
              Icon(Icons.route),
              SizedBox(height: 20),
              Icon(Icons.share),
              SizedBox(height: 20),
            ],
          ),

          Row(
            mainAxisAlignment: MainAxisAlignment.spaceEvenly,
            children: [
              Text('CALL'),
              SizedBox(height: 30),
              Text('ROUTE'),
              SizedBox(height: 30),
              Text('SHARE'),
              SizedBox(height: 30),
            ],
          ),

          SizedBox(height: 30, width: 20),

          Row(
            mainAxisAlignment: MainAxisAlignment.start,
            children: [
              SizedBox(height: 20, width: 40),
              Text('Lorem Ipsum is simply dummy text of rem Ipsum has been the industry. Lorem Ipsum is simply dummy text of rem Ipsum has been the industry. '),
              SizedBox(height: 10),
            ],
          ),

          Row(
            mainAxisAlignment: MainAxisAlignment.start,
            children: [
              SizedBox(height: 20, width: 40),
              Text('Lorem Ipsum is simply dummy text of rem Ipsum has been the industry. Lorem Ipsum is simply dummy text of rem Ipsum has been the industry. '),
              SizedBox(height: 10),
            ],
          ),

          SizedBox(height: 30, width: 20),

          ElevatedButton(onPressed: () {}, child: Text('Submit'))

        ],
      ),
    );
  }
}
